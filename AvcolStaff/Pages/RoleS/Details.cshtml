@page
@model AvcolStaff.Pages.RoleS.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Roles</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Roles.Role)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Roles.Role)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Roles.StaffID">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
