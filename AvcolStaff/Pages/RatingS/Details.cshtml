@page
@model AvcolStaff.Pages.RatingS.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Rating</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Rating.ReviewStars)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Rating.ReviewStars)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Rating.Staff)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Rating.Staff.FirstName)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Rating.StaffID">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
