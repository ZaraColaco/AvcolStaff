@page
@model AvcolStaff.Pages.SalaryS.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Salary[0].StaffID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Salary[0].SalaryGradeName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Salary[0].StartRange)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Salary[0].EndRange)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Salary[0].ActualSalary)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Salary[0].MoeNumber)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Salary) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StaffID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SalaryGradeName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StartRange)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EndRange)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ActualSalary)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MoeNumber)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.SalaryID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.SalaryID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.SalaryID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
