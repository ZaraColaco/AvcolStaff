@page
@model AvcolStaff.Pages.StaffS.CreateModel

@{
    ViewData["Title"] = "Create";
}
<script type="text/javascript">
    function onNameChange() {
        var firstName = $('#Staff_FirstName').val();
        var lastName = $('#Staff_LastName').val();

        if (firstName !== "" && lastName !== "") {
            $('#Staff_TeacherCode').val(firstName.substring(0, 1).toUpperCase() + lastName.substring(0, 2).toUpperCase());
            $('.staffreadonly').val(firstName.substring(0, 1).toUpperCase() + lastName.substring(0, 2).toUpperCase())

        }

    }
</script>

<h1>Create</h1>

<h4>Staff</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Staff.FirstName" class="control-label"></label>
                <input asp-for="Staff.FirstName" class="form-control"onchange="onNameChange()"/>
                <span asp-validation-for="Staff.FirstName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Staff.LastName" class="control-label"></label>
                <input asp-for="Staff.LastName" class="form-control"onchange="onNameChange()"/>
                <span asp-validation-for="Staff.LastName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Staff.TeacherCode" class="control-label"></label>
                <input asp-for="Staff.TeacherCode" readonly class="form-control" />
                <span asp-validation-for="Staff.TeacherCode" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Staff.HireDate" class="control-label"></label>
                <input asp-for="Staff.HireDate" class="form-control" />
                <span asp-validation-for="Staff.HireDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
